spring:
  jpa:
  flyway:
    baseline-on-migrate: true
  datasource:
    url: 'jdbc:tc:mysql:///widokidb'  #containerized Db for running Tests (testContainers)
    username: 'root'
    password: 'root'