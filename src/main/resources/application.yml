spring:
  flyway:
    baseline-on-migrate: true
  datasource:
    url: 'jdbc:mysql://localhost:3306/bootdb?createDatabaseIfNotExist=true'
    username: 'root'
    password: 'root'

logging:
  level:
    org:
      hibernate:
        SQL=debug: